import { TeacherPlaceholder } from '@/components/teacher/teacher-placeholder';

export default function TeacherSettingsPage() {
  return (
    <TeacherPlaceholder
      title="教师设置"
      description="教师模式设置路由已创建，后续会承载默认模型、导出偏好与课堂互动配置。"
    />
  );
}
