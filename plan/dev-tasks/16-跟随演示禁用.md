# 功能点 16：跟随演示功能禁用

| 属性 | 值 |
|------|----|
| 状态 | 待开发 |
| 优先级 | P2 |
| 阶段 | 第三阶段：功能禁用与控制 |
| 前置依赖 | 01-Feature Flag 功能开关系统 |
| 预估工时 | 1-2天 |

---

## 一、功能概述

在教师扩展模式下，通过 Feature Flag 禁用跟随演示功能。跟随演示是指在多人协作场景中，观众端自动跟随演示者的页面动作（翻页、指针移动等）。

### 禁用范围

1. 演示者模式 (Presenter Mode)
2. 跟随者模式 (Follower Mode)
3. 激光笔 (Laser Pointer)
4. 页面动作同步 (Action Sync)
5. 实时光标 (Live Cursor)

---

## 二、现有代码分析

### 跟随演示相关代码（需识别）

| 可能路径 | 说明 |
|---------|------|
| `components/presenter/` | 演示者相关组件 |
| `lib/sync/` | 同步相关逻辑 |
| `lib/follow/` | 跟随逻辑 |
| 实时通信层 | 动作同步事件 |

> 搜索关键词：`presenter`、`follow`、`sync`、`laser`、`cursor`、`pointer`。

---

## 三、详细实现内容

### 3.1 UI 层禁用

```tsx
<FeatureGate feature="followPresenter">
  <PresenterModeButton />
  <FollowButton />
  <LaserPointer />
</FeatureGate>
```

### 3.2 同步事件过滤

在实时通信层，当 Feature Flag 关闭时，不发送和不处理跟随演示相关事件。

### 3.3 演示模式简化

教师扩展模式下的课件展示使用独立的演示模式（见功能点 06），不依赖原有的跟随演示机制。

---

## 四、需要新增的文件

无新增文件。

---

## 五、需要修改的文件

| 文件路径 | 修改内容 |
|---------|----------|
| 演示者模式组件（待确认） | FeatureGate 包装 |
| 跟随按钮组件（待确认） | FeatureGate 包装 |
| 同步逻辑（待确认） | 条件过滤事件 |

---

## 六、验证方案

1. Feature Flag 关闭时无演示者/跟随者入口
2. 不发送同步事件
3. 课件展示使用教师模式独立的演示功能
4. Feature Flag 打开时完全恢复

---

## 七、风险与注意事项

1. **与课件展示的关系**：确保禁用跟随演示不影响教师自身的课件展示
2. **实时通信开销**：禁用后减少不必要的 WebSocket 事件
